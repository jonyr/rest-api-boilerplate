components:
  schemas:
    AccessToken:
      type: object
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
    Registration:
      type: object
      properties:
        email:
          type: string
